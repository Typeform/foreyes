const getUrl = require('./getUrl')

module.exports = (browser, componentName) => {
    const url = getUrl(componentName);
    const imagePath = "";
    const imagesBasePath = './screenshots/taken/'; //ensure your screenshots are put under here

    //your code here

    return imagePath;
}