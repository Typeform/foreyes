{
  "name": "tests",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "install:token": "echo //registry.npmjs.org/:_authToken=${NPM_TOKEN} > .npmrc",
    "server": "yarn katt run-server &",
    "link-katt": "npm link @typeform/katt",
    "test": "yarn server && yarn katt test-all"
  },
  "devDependencies": {
    "@typeform/katt": "^1.4.0",
    "styled-components": "^3.3.3"
  },
  "babel": {
    "presets": [
      "react",
      "env",
      "stage-2"
    ]
  }
}
